<div>
    <h1>All Products</h1>
    

    <ul id="productos">
      {{#each products}}
        <li>{{this.title}} - {{this.price}}</li>
      {{/each}}
    </ul>

</div>

<script src="/js/index.js"></script>